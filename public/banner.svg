
<svg width="1400" height="400" viewBox="0 0 1400 400" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Gradiente del icono -->
    <linearGradient id="g1" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#8B5CF6"/>
      <stop offset="50%" stop-color="#EC4899"/>
      <stop offset="100%" stop-color="#06B6D4"/>
    </linearGradient>
    <style>
      @media (prefers-color-scheme: dark) {
        .bg { fill: #0B0B0F; }
        .text { fill: #FFFFFF; }
        .iconText { fill: #FFFFFF; }
      }
      @media (prefers-color-scheme: light) {
        .bg { fill: #0B0B0F; }
        .text { fill: #FFFFFF; }
        .iconText { fill: #FFFFFF; }
      }
    </style>
  </defs>

  <!-- Fondo -->
  <rect class="bg" width="1400" height="400" rx="24"/>

  <!-- Texto centrado -->
  <text x="700" y="208" text-anchor="middle" dominant-baseline="middle"
        class="text" font-family="Inter, ui-sans-serif, system-ui" font-size="80" font-weight="800">
    Dario Benitez
  </text>

  <!-- Icono (círculo + </>) -->
  <g transform="translate(700,200)">
    <g transform="translate(-340,0)">
      <circle cx="0" cy="0" r="56" fill="url(#g1)"/>
      <text x="0" y="2" text-anchor="middle" dominant-baseline="middle"
            class="iconText" font-family="Inter, ui-sans-serif, system-ui" font-size="38" font-weight="800">
        &lt;/&gt;
      </text>
    </g>
  </g>
</svg>
